server:
  port: 8080

spring:
  application:
    name: UserService
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    spring:
      autoconfigure:
        exclude:
          - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration