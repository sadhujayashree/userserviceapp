server:
  port: 8080

spring:
  application:
    name: UserService
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    management:
      endpoints:
        web:
          exposure:
            include: "*"

    logging:
      level:
        root: INFO